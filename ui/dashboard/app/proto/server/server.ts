// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v3.12.4
// source: server/server.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ValidationError } from "../common/validation";

export const protobufPackage = "server";

export enum StatusCode {
  UNSPECIFIED = 0,
  OK = 200,
  CREATED = 201,
  NOT_FOUND = 404,
  INTERNAL_ERROR = 500,
  BAD_REQUEST = 400,
  VALIDATION_FAILED = 402,
  UNRECOGNIZED = -1,
}

export function statusCodeFromJSON(object: any): StatusCode {
  switch (object) {
    case 0:
    case "UNSPECIFIED":
      return StatusCode.UNSPECIFIED;
    case 200:
    case "OK":
      return StatusCode.OK;
    case 201:
    case "CREATED":
      return StatusCode.CREATED;
    case 404:
    case "NOT_FOUND":
      return StatusCode.NOT_FOUND;
    case 500:
    case "INTERNAL_ERROR":
      return StatusCode.INTERNAL_ERROR;
    case 400:
    case "BAD_REQUEST":
      return StatusCode.BAD_REQUEST;
    case 402:
    case "VALIDATION_FAILED":
      return StatusCode.VALIDATION_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StatusCode.UNRECOGNIZED;
  }
}

export function statusCodeToJSON(object: StatusCode): string {
  switch (object) {
    case StatusCode.UNSPECIFIED:
      return "UNSPECIFIED";
    case StatusCode.OK:
      return "OK";
    case StatusCode.CREATED:
      return "CREATED";
    case StatusCode.NOT_FOUND:
      return "NOT_FOUND";
    case StatusCode.INTERNAL_ERROR:
      return "INTERNAL_ERROR";
    case StatusCode.BAD_REQUEST:
      return "BAD_REQUEST";
    case StatusCode.VALIDATION_FAILED:
      return "VALIDATION_FAILED";
    case StatusCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Status dimana hanya untuk koneksi ke server, apakah koneksi berhasil atau tidak */
export enum StatusServer {
  STATUS_SERVER_UNSPECIFIED = 0,
  CONNECTED = 1,
  DISCONNECTED = 2,
  UNRECOGNIZED = -1,
}

export function statusServerFromJSON(object: any): StatusServer {
  switch (object) {
    case 0:
    case "STATUS_SERVER_UNSPECIFIED":
      return StatusServer.STATUS_SERVER_UNSPECIFIED;
    case 1:
    case "CONNECTED":
      return StatusServer.CONNECTED;
    case 2:
    case "DISCONNECTED":
      return StatusServer.DISCONNECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StatusServer.UNRECOGNIZED;
  }
}

export function statusServerToJSON(object: StatusServer): string {
  switch (object) {
    case StatusServer.STATUS_SERVER_UNSPECIFIED:
      return "STATUS_SERVER_UNSPECIFIED";
    case StatusServer.CONNECTED:
      return "CONNECTED";
    case StatusServer.DISCONNECTED:
      return "DISCONNECTED";
    case StatusServer.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AgentStatusServer {
  AGENT_STATUS_SERVER_UNSPECIFIED = 0,
  INSTALLING = 1,
  CONFIGURING = 2,
  FINALIZING_SETUP = 3,
  SUCCESS = 4,
  FAILED = 5,
  UNRECOGNIZED = -1,
}

export function agentStatusServerFromJSON(object: any): AgentStatusServer {
  switch (object) {
    case 0:
    case "AGENT_STATUS_SERVER_UNSPECIFIED":
      return AgentStatusServer.AGENT_STATUS_SERVER_UNSPECIFIED;
    case 1:
    case "INSTALLING":
      return AgentStatusServer.INSTALLING;
    case 2:
    case "CONFIGURING":
      return AgentStatusServer.CONFIGURING;
    case 3:
    case "FINALIZING_SETUP":
      return AgentStatusServer.FINALIZING_SETUP;
    case 4:
    case "SUCCESS":
      return AgentStatusServer.SUCCESS;
    case 5:
    case "FAILED":
      return AgentStatusServer.FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AgentStatusServer.UNRECOGNIZED;
  }
}

export function agentStatusServerToJSON(object: AgentStatusServer): string {
  switch (object) {
    case AgentStatusServer.AGENT_STATUS_SERVER_UNSPECIFIED:
      return "AGENT_STATUS_SERVER_UNSPECIFIED";
    case AgentStatusServer.INSTALLING:
      return "INSTALLING";
    case AgentStatusServer.CONFIGURING:
      return "CONFIGURING";
    case AgentStatusServer.FINALIZING_SETUP:
      return "FINALIZING_SETUP";
    case AgentStatusServer.SUCCESS:
      return "SUCCESS";
    case AgentStatusServer.FAILED:
      return "FAILED";
    case AgentStatusServer.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Empty {
}

export interface Id {
  id: string;
}

export interface ServerResponse {
  status: StatusCode;
  server: Server | undefined;
  errors: ValidationError[];
  error?: string | undefined;
}

export interface ServersResponse {
  status: StatusCode;
  servers: Server[];
  errors: ValidationError[];
  error?: string | undefined;
}

export interface Server {
  id: string;
  name: string;
  ipAddress: string;
  port: number;
  protocol: string;
  credential: ServerCredential | undefined;
  isRoot: number;
}

export interface ServerCredential {
  username: string;
  password?: string | undefined;
  sshKey?: string | undefined;
}

export interface MessageResponse {
  status: StatusCode;
  message: string;
}

function createBaseEmpty(): Empty {
  return {};
}

export const Empty: MessageFns<Empty> = {
  encode(_: Empty, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Empty {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Empty {
    return {};
  },

  toJSON(_: Empty): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Empty>, I>>(base?: I): Empty {
    return Empty.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Empty>, I>>(_: I): Empty {
    const message = createBaseEmpty();
    return message;
  },
};

function createBaseId(): Id {
  return { id: "" };
}

export const Id: MessageFns<Id> = {
  encode(message: Id, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Id {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Id {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: Id): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Id>, I>>(base?: I): Id {
    return Id.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Id>, I>>(object: I): Id {
    const message = createBaseId();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseServerResponse(): ServerResponse {
  return { status: 0, server: undefined, errors: [], error: undefined };
}

export const ServerResponse: MessageFns<ServerResponse> = {
  encode(message: ServerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.server !== undefined) {
      Server.encode(message.server, writer.uint32(18).fork()).join();
    }
    for (const v of message.errors) {
      ValidationError.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.error !== undefined) {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.server = Server.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(ValidationError.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerResponse {
    return {
      status: isSet(object.status) ? statusCodeFromJSON(object.status) : 0,
      server: isSet(object.server) ? Server.fromJSON(object.server) : undefined,
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => ValidationError.fromJSON(e))
        : [],
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: ServerResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = statusCodeToJSON(message.status);
    }
    if (message.server !== undefined) {
      obj.server = Server.toJSON(message.server);
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => ValidationError.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerResponse>, I>>(base?: I): ServerResponse {
    return ServerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerResponse>, I>>(object: I): ServerResponse {
    const message = createBaseServerResponse();
    message.status = object.status ?? 0;
    message.server = (object.server !== undefined && object.server !== null)
      ? Server.fromPartial(object.server)
      : undefined;
    message.errors = object.errors?.map((e) => ValidationError.fromPartial(e)) || [];
    message.error = object.error ?? undefined;
    return message;
  },
};

function createBaseServersResponse(): ServersResponse {
  return { status: 0, servers: [], errors: [], error: undefined };
}

export const ServersResponse: MessageFns<ServersResponse> = {
  encode(message: ServersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    for (const v of message.servers) {
      Server.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.errors) {
      ValidationError.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.error !== undefined) {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.servers.push(Server.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(ValidationError.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServersResponse {
    return {
      status: isSet(object.status) ? statusCodeFromJSON(object.status) : 0,
      servers: globalThis.Array.isArray(object?.servers) ? object.servers.map((e: any) => Server.fromJSON(e)) : [],
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => ValidationError.fromJSON(e))
        : [],
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: ServersResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = statusCodeToJSON(message.status);
    }
    if (message.servers?.length) {
      obj.servers = message.servers.map((e) => Server.toJSON(e));
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => ValidationError.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServersResponse>, I>>(base?: I): ServersResponse {
    return ServersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServersResponse>, I>>(object: I): ServersResponse {
    const message = createBaseServersResponse();
    message.status = object.status ?? 0;
    message.servers = object.servers?.map((e) => Server.fromPartial(e)) || [];
    message.errors = object.errors?.map((e) => ValidationError.fromPartial(e)) || [];
    message.error = object.error ?? undefined;
    return message;
  },
};

function createBaseServer(): Server {
  return { id: "", name: "", ipAddress: "", port: 0, protocol: "", credential: undefined, isRoot: 0 };
}

export const Server: MessageFns<Server> = {
  encode(message: Server, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.ipAddress !== "") {
      writer.uint32(26).string(message.ipAddress);
    }
    if (message.port !== 0) {
      writer.uint32(32).int32(message.port);
    }
    if (message.protocol !== "") {
      writer.uint32(42).string(message.protocol);
    }
    if (message.credential !== undefined) {
      ServerCredential.encode(message.credential, writer.uint32(50).fork()).join();
    }
    if (message.isRoot !== 0) {
      writer.uint32(56).int32(message.isRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Server {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ipAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.credential = ServerCredential.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.isRoot = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Server {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      ipAddress: isSet(object.ipAddress) ? globalThis.String(object.ipAddress) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      credential: isSet(object.credential) ? ServerCredential.fromJSON(object.credential) : undefined,
      isRoot: isSet(object.isRoot) ? globalThis.Number(object.isRoot) : 0,
    };
  },

  toJSON(message: Server): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.ipAddress !== "") {
      obj.ipAddress = message.ipAddress;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.credential !== undefined) {
      obj.credential = ServerCredential.toJSON(message.credential);
    }
    if (message.isRoot !== 0) {
      obj.isRoot = Math.round(message.isRoot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Server>, I>>(base?: I): Server {
    return Server.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Server>, I>>(object: I): Server {
    const message = createBaseServer();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.ipAddress = object.ipAddress ?? "";
    message.port = object.port ?? 0;
    message.protocol = object.protocol ?? "";
    message.credential = (object.credential !== undefined && object.credential !== null)
      ? ServerCredential.fromPartial(object.credential)
      : undefined;
    message.isRoot = object.isRoot ?? 0;
    return message;
  },
};

function createBaseServerCredential(): ServerCredential {
  return { username: "", password: undefined, sshKey: undefined };
}

export const ServerCredential: MessageFns<ServerCredential> = {
  encode(message: ServerCredential, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.password !== undefined) {
      writer.uint32(18).string(message.password);
    }
    if (message.sshKey !== undefined) {
      writer.uint32(26).string(message.sshKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerCredential {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerCredential();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sshKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerCredential {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : undefined,
      sshKey: isSet(object.sshKey) ? globalThis.String(object.sshKey) : undefined,
    };
  },

  toJSON(message: ServerCredential): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== undefined) {
      obj.password = message.password;
    }
    if (message.sshKey !== undefined) {
      obj.sshKey = message.sshKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerCredential>, I>>(base?: I): ServerCredential {
    return ServerCredential.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerCredential>, I>>(object: I): ServerCredential {
    const message = createBaseServerCredential();
    message.username = object.username ?? "";
    message.password = object.password ?? undefined;
    message.sshKey = object.sshKey ?? undefined;
    return message;
  },
};

function createBaseMessageResponse(): MessageResponse {
  return { status: 0, message: "" };
}

export const MessageResponse: MessageFns<MessageResponse> = {
  encode(message: MessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageResponse {
    return {
      status: isSet(object.status) ? statusCodeFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: MessageResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = statusCodeToJSON(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageResponse>, I>>(base?: I): MessageResponse {
    return MessageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageResponse>, I>>(object: I): MessageResponse {
    const message = createBaseMessageResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

export interface ServerService {
  Create(request: Server): Promise<ServerResponse>;
  Get(request: Id): Promise<ServerResponse>;
  All(request: Empty): Promise<ServersResponse>;
  Update(request: Server): Promise<ServerResponse>;
  Delete(request: Id): Promise<MessageResponse>;
  InstallAgent(request: Id): Promise<MessageResponse>;
}

export const ServerServiceServiceName = "server.ServerService";
export class ServerServiceClientImpl implements ServerService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ServerServiceServiceName;
    this.rpc = rpc;
    this.Create = this.Create.bind(this);
    this.Get = this.Get.bind(this);
    this.All = this.All.bind(this);
    this.Update = this.Update.bind(this);
    this.Delete = this.Delete.bind(this);
    this.InstallAgent = this.InstallAgent.bind(this);
  }
  Create(request: Server): Promise<ServerResponse> {
    const data = Server.encode(request).finish();
    const promise = this.rpc.request(this.service, "Create", data);
    return promise.then((data) => ServerResponse.decode(new BinaryReader(data)));
  }

  Get(request: Id): Promise<ServerResponse> {
    const data = Id.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => ServerResponse.decode(new BinaryReader(data)));
  }

  All(request: Empty): Promise<ServersResponse> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "All", data);
    return promise.then((data) => ServersResponse.decode(new BinaryReader(data)));
  }

  Update(request: Server): Promise<ServerResponse> {
    const data = Server.encode(request).finish();
    const promise = this.rpc.request(this.service, "Update", data);
    return promise.then((data) => ServerResponse.decode(new BinaryReader(data)));
  }

  Delete(request: Id): Promise<MessageResponse> {
    const data = Id.encode(request).finish();
    const promise = this.rpc.request(this.service, "Delete", data);
    return promise.then((data) => MessageResponse.decode(new BinaryReader(data)));
  }

  InstallAgent(request: Id): Promise<MessageResponse> {
    const data = Id.encode(request).finish();
    const promise = this.rpc.request(this.service, "InstallAgent", data);
    return promise.then((data) => MessageResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
