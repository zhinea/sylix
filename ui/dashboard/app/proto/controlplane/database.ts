// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v3.12.4
// source: controlplane/database.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Empty } from "../common/common";

export const protobufPackage = "controlplane";

export interface DatabaseId {
  id: string;
}

export interface Database {
  id: string;
  name: string;
  user: string;
  password: string;
  dbName: string;
  branch: string;
  serverId: string;
  status: string;
  containerId: string;
  port: number;
}

export interface DatabaseResponse {
  database: Database | undefined;
}

export interface DatabasesResponse {
  databases: Database[];
}

export interface DatabaseMessageResponse {
  status: number;
  message: string;
}

function createBaseDatabaseId(): DatabaseId {
  return { id: "" };
}

export const DatabaseId: MessageFns<DatabaseId> = {
  encode(message: DatabaseId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatabaseId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabaseId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatabaseId {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: DatabaseId): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatabaseId>, I>>(base?: I): DatabaseId {
    return DatabaseId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatabaseId>, I>>(object: I): DatabaseId {
    const message = createBaseDatabaseId();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseDatabase(): Database {
  return {
    id: "",
    name: "",
    user: "",
    password: "",
    dbName: "",
    branch: "",
    serverId: "",
    status: "",
    containerId: "",
    port: 0,
  };
}

export const Database: MessageFns<Database> = {
  encode(message: Database, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.user !== "") {
      writer.uint32(26).string(message.user);
    }
    if (message.password !== "") {
      writer.uint32(34).string(message.password);
    }
    if (message.dbName !== "") {
      writer.uint32(42).string(message.dbName);
    }
    if (message.branch !== "") {
      writer.uint32(50).string(message.branch);
    }
    if (message.serverId !== "") {
      writer.uint32(58).string(message.serverId);
    }
    if (message.status !== "") {
      writer.uint32(66).string(message.status);
    }
    if (message.containerId !== "") {
      writer.uint32(74).string(message.containerId);
    }
    if (message.port !== 0) {
      writer.uint32(80).int32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Database {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dbName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Database {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      dbName: isSet(object.dbName) ? globalThis.String(object.dbName) : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      serverId: isSet(object.serverId) ? globalThis.String(object.serverId) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      containerId: isSet(object.containerId) ? globalThis.String(object.containerId) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: Database): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.dbName !== "") {
      obj.dbName = message.dbName;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Database>, I>>(base?: I): Database {
    return Database.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Database>, I>>(object: I): Database {
    const message = createBaseDatabase();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.user = object.user ?? "";
    message.password = object.password ?? "";
    message.dbName = object.dbName ?? "";
    message.branch = object.branch ?? "";
    message.serverId = object.serverId ?? "";
    message.status = object.status ?? "";
    message.containerId = object.containerId ?? "";
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseDatabaseResponse(): DatabaseResponse {
  return { database: undefined };
}

export const DatabaseResponse: MessageFns<DatabaseResponse> = {
  encode(message: DatabaseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.database !== undefined) {
      Database.encode(message.database, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatabaseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.database = Database.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatabaseResponse {
    return { database: isSet(object.database) ? Database.fromJSON(object.database) : undefined };
  },

  toJSON(message: DatabaseResponse): unknown {
    const obj: any = {};
    if (message.database !== undefined) {
      obj.database = Database.toJSON(message.database);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatabaseResponse>, I>>(base?: I): DatabaseResponse {
    return DatabaseResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatabaseResponse>, I>>(object: I): DatabaseResponse {
    const message = createBaseDatabaseResponse();
    message.database = (object.database !== undefined && object.database !== null)
      ? Database.fromPartial(object.database)
      : undefined;
    return message;
  },
};

function createBaseDatabasesResponse(): DatabasesResponse {
  return { databases: [] };
}

export const DatabasesResponse: MessageFns<DatabasesResponse> = {
  encode(message: DatabasesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.databases) {
      Database.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatabasesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabasesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.databases.push(Database.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatabasesResponse {
    return {
      databases: globalThis.Array.isArray(object?.databases)
        ? object.databases.map((e: any) => Database.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DatabasesResponse): unknown {
    const obj: any = {};
    if (message.databases?.length) {
      obj.databases = message.databases.map((e) => Database.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatabasesResponse>, I>>(base?: I): DatabasesResponse {
    return DatabasesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatabasesResponse>, I>>(object: I): DatabasesResponse {
    const message = createBaseDatabasesResponse();
    message.databases = object.databases?.map((e) => Database.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDatabaseMessageResponse(): DatabaseMessageResponse {
  return { status: 0, message: "" };
}

export const DatabaseMessageResponse: MessageFns<DatabaseMessageResponse> = {
  encode(message: DatabaseMessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatabaseMessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabaseMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatabaseMessageResponse {
    return {
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: DatabaseMessageResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatabaseMessageResponse>, I>>(base?: I): DatabaseMessageResponse {
    return DatabaseMessageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatabaseMessageResponse>, I>>(object: I): DatabaseMessageResponse {
    const message = createBaseDatabaseMessageResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

export interface DatabaseService {
  Create(request: Database): Promise<DatabaseResponse>;
  Get(request: DatabaseId): Promise<DatabaseResponse>;
  All(request: Empty): Promise<DatabasesResponse>;
  Delete(request: DatabaseId): Promise<DatabaseMessageResponse>;
}

export const DatabaseServiceServiceName = "controlplane.DatabaseService";
export class DatabaseServiceClientImpl implements DatabaseService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || DatabaseServiceServiceName;
    this.rpc = rpc;
    this.Create = this.Create.bind(this);
    this.Get = this.Get.bind(this);
    this.All = this.All.bind(this);
    this.Delete = this.Delete.bind(this);
  }
  Create(request: Database): Promise<DatabaseResponse> {
    const data = Database.encode(request).finish();
    const promise = this.rpc.request(this.service, "Create", data);
    return promise.then((data) => DatabaseResponse.decode(new BinaryReader(data)));
  }

  Get(request: DatabaseId): Promise<DatabaseResponse> {
    const data = DatabaseId.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => DatabaseResponse.decode(new BinaryReader(data)));
  }

  All(request: Empty): Promise<DatabasesResponse> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "All", data);
    return promise.then((data) => DatabasesResponse.decode(new BinaryReader(data)));
  }

  Delete(request: DatabaseId): Promise<DatabaseMessageResponse> {
    const data = DatabaseId.encode(request).finish();
    const promise = this.rpc.request(this.service, "Delete", data);
    return promise.then((data) => DatabaseMessageResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
