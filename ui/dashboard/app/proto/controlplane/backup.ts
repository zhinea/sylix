// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v3.12.4
// source: controlplane/backup.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Empty } from "../common/common";
import { ValidationError } from "../common/validation";

export const protobufPackage = "controlplane";

export enum BackupStatusCode {
  BACKUP_UNSPECIFIED = 0,
  BACKUP_OK = 200,
  BACKUP_CREATED = 201,
  BACKUP_NOT_FOUND = 404,
  BACKUP_INTERNAL_ERROR = 500,
  BACKUP_BAD_REQUEST = 400,
  UNRECOGNIZED = -1,
}

export function backupStatusCodeFromJSON(object: any): BackupStatusCode {
  switch (object) {
    case 0:
    case "BACKUP_UNSPECIFIED":
      return BackupStatusCode.BACKUP_UNSPECIFIED;
    case 200:
    case "BACKUP_OK":
      return BackupStatusCode.BACKUP_OK;
    case 201:
    case "BACKUP_CREATED":
      return BackupStatusCode.BACKUP_CREATED;
    case 404:
    case "BACKUP_NOT_FOUND":
      return BackupStatusCode.BACKUP_NOT_FOUND;
    case 500:
    case "BACKUP_INTERNAL_ERROR":
      return BackupStatusCode.BACKUP_INTERNAL_ERROR;
    case 400:
    case "BACKUP_BAD_REQUEST":
      return BackupStatusCode.BACKUP_BAD_REQUEST;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BackupStatusCode.UNRECOGNIZED;
  }
}

export function backupStatusCodeToJSON(object: BackupStatusCode): string {
  switch (object) {
    case BackupStatusCode.BACKUP_UNSPECIFIED:
      return "BACKUP_UNSPECIFIED";
    case BackupStatusCode.BACKUP_OK:
      return "BACKUP_OK";
    case BackupStatusCode.BACKUP_CREATED:
      return "BACKUP_CREATED";
    case BackupStatusCode.BACKUP_NOT_FOUND:
      return "BACKUP_NOT_FOUND";
    case BackupStatusCode.BACKUP_INTERNAL_ERROR:
      return "BACKUP_INTERNAL_ERROR";
    case BackupStatusCode.BACKUP_BAD_REQUEST:
      return "BACKUP_BAD_REQUEST";
    case BackupStatusCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface BackupStorageId {
  id: string;
}

export interface BackupMessageResponse {
  status: BackupStatusCode;
  message: string;
}

export interface BackupStorage {
  id: string;
  name: string;
  endpoint: string;
  region: string;
  bucket: string;
  accessKey: string;
  secretKey: string;
  /** CONNECTED, ERROR */
  status: string;
  errorMessage: string;
}

export interface BackupStorageResponse {
  data: BackupStorage | undefined;
  status: BackupStatusCode;
  errors: ValidationError[];
  error?: string | undefined;
}

export interface BackupStoragesResponse {
  data: BackupStorage[];
  status: BackupStatusCode;
}

function createBaseBackupStorageId(): BackupStorageId {
  return { id: "" };
}

export const BackupStorageId: MessageFns<BackupStorageId> = {
  encode(message: BackupStorageId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackupStorageId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackupStorageId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackupStorageId {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: BackupStorageId): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackupStorageId>, I>>(base?: I): BackupStorageId {
    return BackupStorageId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackupStorageId>, I>>(object: I): BackupStorageId {
    const message = createBaseBackupStorageId();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseBackupMessageResponse(): BackupMessageResponse {
  return { status: 0, message: "" };
}

export const BackupMessageResponse: MessageFns<BackupMessageResponse> = {
  encode(message: BackupMessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackupMessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackupMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackupMessageResponse {
    return {
      status: isSet(object.status) ? backupStatusCodeFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: BackupMessageResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = backupStatusCodeToJSON(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackupMessageResponse>, I>>(base?: I): BackupMessageResponse {
    return BackupMessageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackupMessageResponse>, I>>(object: I): BackupMessageResponse {
    const message = createBaseBackupMessageResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseBackupStorage(): BackupStorage {
  return {
    id: "",
    name: "",
    endpoint: "",
    region: "",
    bucket: "",
    accessKey: "",
    secretKey: "",
    status: "",
    errorMessage: "",
  };
}

export const BackupStorage: MessageFns<BackupStorage> = {
  encode(message: BackupStorage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.endpoint !== "") {
      writer.uint32(26).string(message.endpoint);
    }
    if (message.region !== "") {
      writer.uint32(34).string(message.region);
    }
    if (message.bucket !== "") {
      writer.uint32(42).string(message.bucket);
    }
    if (message.accessKey !== "") {
      writer.uint32(50).string(message.accessKey);
    }
    if (message.secretKey !== "") {
      writer.uint32(58).string(message.secretKey);
    }
    if (message.status !== "") {
      writer.uint32(66).string(message.status);
    }
    if (message.errorMessage !== "") {
      writer.uint32(74).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackupStorage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackupStorage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.bucket = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.accessKey = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.secretKey = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackupStorage {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      bucket: isSet(object.bucket) ? globalThis.String(object.bucket) : "",
      accessKey: isSet(object.accessKey) ? globalThis.String(object.accessKey) : "",
      secretKey: isSet(object.secretKey) ? globalThis.String(object.secretKey) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
    };
  },

  toJSON(message: BackupStorage): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.bucket !== "") {
      obj.bucket = message.bucket;
    }
    if (message.accessKey !== "") {
      obj.accessKey = message.accessKey;
    }
    if (message.secretKey !== "") {
      obj.secretKey = message.secretKey;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackupStorage>, I>>(base?: I): BackupStorage {
    return BackupStorage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackupStorage>, I>>(object: I): BackupStorage {
    const message = createBaseBackupStorage();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.endpoint = object.endpoint ?? "";
    message.region = object.region ?? "";
    message.bucket = object.bucket ?? "";
    message.accessKey = object.accessKey ?? "";
    message.secretKey = object.secretKey ?? "";
    message.status = object.status ?? "";
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseBackupStorageResponse(): BackupStorageResponse {
  return { data: undefined, status: 0, errors: [], error: undefined };
}

export const BackupStorageResponse: MessageFns<BackupStorageResponse> = {
  encode(message: BackupStorageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      BackupStorage.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.errors) {
      ValidationError.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.error !== undefined) {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackupStorageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackupStorageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = BackupStorage.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(ValidationError.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackupStorageResponse {
    return {
      data: isSet(object.data) ? BackupStorage.fromJSON(object.data) : undefined,
      status: isSet(object.status) ? backupStatusCodeFromJSON(object.status) : 0,
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => ValidationError.fromJSON(e))
        : [],
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: BackupStorageResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = BackupStorage.toJSON(message.data);
    }
    if (message.status !== 0) {
      obj.status = backupStatusCodeToJSON(message.status);
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => ValidationError.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackupStorageResponse>, I>>(base?: I): BackupStorageResponse {
    return BackupStorageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackupStorageResponse>, I>>(object: I): BackupStorageResponse {
    const message = createBaseBackupStorageResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? BackupStorage.fromPartial(object.data)
      : undefined;
    message.status = object.status ?? 0;
    message.errors = object.errors?.map((e) => ValidationError.fromPartial(e)) || [];
    message.error = object.error ?? undefined;
    return message;
  },
};

function createBaseBackupStoragesResponse(): BackupStoragesResponse {
  return { data: [], status: 0 };
}

export const BackupStoragesResponse: MessageFns<BackupStoragesResponse> = {
  encode(message: BackupStoragesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      BackupStorage.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackupStoragesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackupStoragesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(BackupStorage.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackupStoragesResponse {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => BackupStorage.fromJSON(e)) : [],
      status: isSet(object.status) ? backupStatusCodeFromJSON(object.status) : 0,
    };
  },

  toJSON(message: BackupStoragesResponse): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => BackupStorage.toJSON(e));
    }
    if (message.status !== 0) {
      obj.status = backupStatusCodeToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackupStoragesResponse>, I>>(base?: I): BackupStoragesResponse {
    return BackupStoragesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackupStoragesResponse>, I>>(object: I): BackupStoragesResponse {
    const message = createBaseBackupStoragesResponse();
    message.data = object.data?.map((e) => BackupStorage.fromPartial(e)) || [];
    message.status = object.status ?? 0;
    return message;
  },
};

export interface BackupStorageService {
  Create(request: BackupStorage): Promise<BackupStorageResponse>;
  Get(request: BackupStorageId): Promise<BackupStorageResponse>;
  All(request: Empty): Promise<BackupStoragesResponse>;
  Update(request: BackupStorage): Promise<BackupStorageResponse>;
  Delete(request: BackupStorageId): Promise<BackupMessageResponse>;
  TestConnection(request: BackupStorage): Promise<BackupMessageResponse>;
}

export const BackupStorageServiceServiceName = "controlplane.BackupStorageService";
export class BackupStorageServiceClientImpl implements BackupStorageService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || BackupStorageServiceServiceName;
    this.rpc = rpc;
    this.Create = this.Create.bind(this);
    this.Get = this.Get.bind(this);
    this.All = this.All.bind(this);
    this.Update = this.Update.bind(this);
    this.Delete = this.Delete.bind(this);
    this.TestConnection = this.TestConnection.bind(this);
  }
  Create(request: BackupStorage): Promise<BackupStorageResponse> {
    const data = BackupStorage.encode(request).finish();
    const promise = this.rpc.request(this.service, "Create", data);
    return promise.then((data) => BackupStorageResponse.decode(new BinaryReader(data)));
  }

  Get(request: BackupStorageId): Promise<BackupStorageResponse> {
    const data = BackupStorageId.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => BackupStorageResponse.decode(new BinaryReader(data)));
  }

  All(request: Empty): Promise<BackupStoragesResponse> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "All", data);
    return promise.then((data) => BackupStoragesResponse.decode(new BinaryReader(data)));
  }

  Update(request: BackupStorage): Promise<BackupStorageResponse> {
    const data = BackupStorage.encode(request).finish();
    const promise = this.rpc.request(this.service, "Update", data);
    return promise.then((data) => BackupStorageResponse.decode(new BinaryReader(data)));
  }

  Delete(request: BackupStorageId): Promise<BackupMessageResponse> {
    const data = BackupStorageId.encode(request).finish();
    const promise = this.rpc.request(this.service, "Delete", data);
    return promise.then((data) => BackupMessageResponse.decode(new BinaryReader(data)));
  }

  TestConnection(request: BackupStorage): Promise<BackupMessageResponse> {
    const data = BackupStorage.encode(request).finish();
    const promise = this.rpc.request(this.service, "TestConnection", data);
    return promise.then((data) => BackupMessageResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
