// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v3.12.4
// source: logs/logs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "logs";

export interface Empty {
}

export interface LogFile {
  name: string;
  size: number;
  lastModified: string;
}

export interface GetSystemLogsResponse {
  files: LogFile[];
}

export interface ReadSystemLogRequest {
  filename: string;
  page: number;
  pageSize: number;
}

export interface ReadSystemLogResponse {
  lines: string[];
  totalLines: number;
  currentPage: number;
  totalPages: number;
}

export interface GetServerLogsRequest {
  serverId: string;
}

export interface GetServerLogsResponse {
  files: LogFile[];
}

export interface ReadServerLogRequest {
  serverId: string;
  filename: string;
  page: number;
  pageSize: number;
}

export interface ReadServerLogResponse {
  lines: string[];
  totalLines: number;
  currentPage: number;
  totalPages: number;
}

function createBaseEmpty(): Empty {
  return {};
}

export const Empty: MessageFns<Empty> = {
  encode(_: Empty, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Empty {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Empty {
    return {};
  },

  toJSON(_: Empty): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Empty>, I>>(base?: I): Empty {
    return Empty.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Empty>, I>>(_: I): Empty {
    const message = createBaseEmpty();
    return message;
  },
};

function createBaseLogFile(): LogFile {
  return { name: "", size: 0, lastModified: "" };
}

export const LogFile: MessageFns<LogFile> = {
  encode(message: LogFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.size !== 0) {
      writer.uint32(16).int64(message.size);
    }
    if (message.lastModified !== "") {
      writer.uint32(26).string(message.lastModified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastModified = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogFile {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      lastModified: isSet(object.lastModified) ? globalThis.String(object.lastModified) : "",
    };
  },

  toJSON(message: LogFile): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.lastModified !== "") {
      obj.lastModified = message.lastModified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogFile>, I>>(base?: I): LogFile {
    return LogFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogFile>, I>>(object: I): LogFile {
    const message = createBaseLogFile();
    message.name = object.name ?? "";
    message.size = object.size ?? 0;
    message.lastModified = object.lastModified ?? "";
    return message;
  },
};

function createBaseGetSystemLogsResponse(): GetSystemLogsResponse {
  return { files: [] };
}

export const GetSystemLogsResponse: MessageFns<GetSystemLogsResponse> = {
  encode(message: GetSystemLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      LogFile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSystemLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSystemLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(LogFile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSystemLogsResponse {
    return { files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => LogFile.fromJSON(e)) : [] };
  },

  toJSON(message: GetSystemLogsResponse): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => LogFile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSystemLogsResponse>, I>>(base?: I): GetSystemLogsResponse {
    return GetSystemLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSystemLogsResponse>, I>>(object: I): GetSystemLogsResponse {
    const message = createBaseGetSystemLogsResponse();
    message.files = object.files?.map((e) => LogFile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReadSystemLogRequest(): ReadSystemLogRequest {
  return { filename: "", page: 0, pageSize: 0 };
}

export const ReadSystemLogRequest: MessageFns<ReadSystemLogRequest> = {
  encode(message: ReadSystemLogRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filename !== "") {
      writer.uint32(10).string(message.filename);
    }
    if (message.page !== 0) {
      writer.uint32(16).int32(message.page);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadSystemLogRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadSystemLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadSystemLogRequest {
    return {
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
    };
  },

  toJSON(message: ReadSystemLogRequest): unknown {
    const obj: any = {};
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadSystemLogRequest>, I>>(base?: I): ReadSystemLogRequest {
    return ReadSystemLogRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadSystemLogRequest>, I>>(object: I): ReadSystemLogRequest {
    const message = createBaseReadSystemLogRequest();
    message.filename = object.filename ?? "";
    message.page = object.page ?? 0;
    message.pageSize = object.pageSize ?? 0;
    return message;
  },
};

function createBaseReadSystemLogResponse(): ReadSystemLogResponse {
  return { lines: [], totalLines: 0, currentPage: 0, totalPages: 0 };
}

export const ReadSystemLogResponse: MessageFns<ReadSystemLogResponse> = {
  encode(message: ReadSystemLogResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.lines) {
      writer.uint32(10).string(v!);
    }
    if (message.totalLines !== 0) {
      writer.uint32(16).int32(message.totalLines);
    }
    if (message.currentPage !== 0) {
      writer.uint32(24).int32(message.currentPage);
    }
    if (message.totalPages !== 0) {
      writer.uint32(32).int32(message.totalPages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadSystemLogResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadSystemLogResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lines.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalLines = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.currentPage = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadSystemLogResponse {
    return {
      lines: globalThis.Array.isArray(object?.lines) ? object.lines.map((e: any) => globalThis.String(e)) : [],
      totalLines: isSet(object.totalLines) ? globalThis.Number(object.totalLines) : 0,
      currentPage: isSet(object.currentPage) ? globalThis.Number(object.currentPage) : 0,
      totalPages: isSet(object.totalPages) ? globalThis.Number(object.totalPages) : 0,
    };
  },

  toJSON(message: ReadSystemLogResponse): unknown {
    const obj: any = {};
    if (message.lines?.length) {
      obj.lines = message.lines;
    }
    if (message.totalLines !== 0) {
      obj.totalLines = Math.round(message.totalLines);
    }
    if (message.currentPage !== 0) {
      obj.currentPage = Math.round(message.currentPage);
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadSystemLogResponse>, I>>(base?: I): ReadSystemLogResponse {
    return ReadSystemLogResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadSystemLogResponse>, I>>(object: I): ReadSystemLogResponse {
    const message = createBaseReadSystemLogResponse();
    message.lines = object.lines?.map((e) => e) || [];
    message.totalLines = object.totalLines ?? 0;
    message.currentPage = object.currentPage ?? 0;
    message.totalPages = object.totalPages ?? 0;
    return message;
  },
};

function createBaseGetServerLogsRequest(): GetServerLogsRequest {
  return { serverId: "" };
}

export const GetServerLogsRequest: MessageFns<GetServerLogsRequest> = {
  encode(message: GetServerLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServerLogsRequest {
    return { serverId: isSet(object.serverId) ? globalThis.String(object.serverId) : "" };
  },

  toJSON(message: GetServerLogsRequest): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerLogsRequest>, I>>(base?: I): GetServerLogsRequest {
    return GetServerLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerLogsRequest>, I>>(object: I): GetServerLogsRequest {
    const message = createBaseGetServerLogsRequest();
    message.serverId = object.serverId ?? "";
    return message;
  },
};

function createBaseGetServerLogsResponse(): GetServerLogsResponse {
  return { files: [] };
}

export const GetServerLogsResponse: MessageFns<GetServerLogsResponse> = {
  encode(message: GetServerLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      LogFile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(LogFile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServerLogsResponse {
    return { files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => LogFile.fromJSON(e)) : [] };
  },

  toJSON(message: GetServerLogsResponse): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => LogFile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServerLogsResponse>, I>>(base?: I): GetServerLogsResponse {
    return GetServerLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerLogsResponse>, I>>(object: I): GetServerLogsResponse {
    const message = createBaseGetServerLogsResponse();
    message.files = object.files?.map((e) => LogFile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReadServerLogRequest(): ReadServerLogRequest {
  return { serverId: "", filename: "", page: 0, pageSize: 0 };
}

export const ReadServerLogRequest: MessageFns<ReadServerLogRequest> = {
  encode(message: ReadServerLogRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    if (message.filename !== "") {
      writer.uint32(18).string(message.filename);
    }
    if (message.page !== 0) {
      writer.uint32(24).int32(message.page);
    }
    if (message.pageSize !== 0) {
      writer.uint32(32).int32(message.pageSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadServerLogRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadServerLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadServerLogRequest {
    return {
      serverId: isSet(object.serverId) ? globalThis.String(object.serverId) : "",
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
    };
  },

  toJSON(message: ReadServerLogRequest): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadServerLogRequest>, I>>(base?: I): ReadServerLogRequest {
    return ReadServerLogRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadServerLogRequest>, I>>(object: I): ReadServerLogRequest {
    const message = createBaseReadServerLogRequest();
    message.serverId = object.serverId ?? "";
    message.filename = object.filename ?? "";
    message.page = object.page ?? 0;
    message.pageSize = object.pageSize ?? 0;
    return message;
  },
};

function createBaseReadServerLogResponse(): ReadServerLogResponse {
  return { lines: [], totalLines: 0, currentPage: 0, totalPages: 0 };
}

export const ReadServerLogResponse: MessageFns<ReadServerLogResponse> = {
  encode(message: ReadServerLogResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.lines) {
      writer.uint32(10).string(v!);
    }
    if (message.totalLines !== 0) {
      writer.uint32(16).int32(message.totalLines);
    }
    if (message.currentPage !== 0) {
      writer.uint32(24).int32(message.currentPage);
    }
    if (message.totalPages !== 0) {
      writer.uint32(32).int32(message.totalPages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadServerLogResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadServerLogResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lines.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalLines = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.currentPage = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadServerLogResponse {
    return {
      lines: globalThis.Array.isArray(object?.lines) ? object.lines.map((e: any) => globalThis.String(e)) : [],
      totalLines: isSet(object.totalLines) ? globalThis.Number(object.totalLines) : 0,
      currentPage: isSet(object.currentPage) ? globalThis.Number(object.currentPage) : 0,
      totalPages: isSet(object.totalPages) ? globalThis.Number(object.totalPages) : 0,
    };
  },

  toJSON(message: ReadServerLogResponse): unknown {
    const obj: any = {};
    if (message.lines?.length) {
      obj.lines = message.lines;
    }
    if (message.totalLines !== 0) {
      obj.totalLines = Math.round(message.totalLines);
    }
    if (message.currentPage !== 0) {
      obj.currentPage = Math.round(message.currentPage);
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadServerLogResponse>, I>>(base?: I): ReadServerLogResponse {
    return ReadServerLogResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadServerLogResponse>, I>>(object: I): ReadServerLogResponse {
    const message = createBaseReadServerLogResponse();
    message.lines = object.lines?.map((e) => e) || [];
    message.totalLines = object.totalLines ?? 0;
    message.currentPage = object.currentPage ?? 0;
    message.totalPages = object.totalPages ?? 0;
    return message;
  },
};

export interface LogsService {
  GetSystemLogs(request: Empty): Promise<GetSystemLogsResponse>;
  ReadSystemLog(request: ReadSystemLogRequest): Promise<ReadSystemLogResponse>;
  GetServerLogs(request: GetServerLogsRequest): Promise<GetServerLogsResponse>;
  ReadServerLog(request: ReadServerLogRequest): Promise<ReadServerLogResponse>;
}

export const LogsServiceServiceName = "logs.LogsService";
export class LogsServiceClientImpl implements LogsService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || LogsServiceServiceName;
    this.rpc = rpc;
    this.GetSystemLogs = this.GetSystemLogs.bind(this);
    this.ReadSystemLog = this.ReadSystemLog.bind(this);
    this.GetServerLogs = this.GetServerLogs.bind(this);
    this.ReadServerLog = this.ReadServerLog.bind(this);
  }
  GetSystemLogs(request: Empty): Promise<GetSystemLogsResponse> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetSystemLogs", data);
    return promise.then((data) => GetSystemLogsResponse.decode(new BinaryReader(data)));
  }

  ReadSystemLog(request: ReadSystemLogRequest): Promise<ReadSystemLogResponse> {
    const data = ReadSystemLogRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ReadSystemLog", data);
    return promise.then((data) => ReadSystemLogResponse.decode(new BinaryReader(data)));
  }

  GetServerLogs(request: GetServerLogsRequest): Promise<GetServerLogsResponse> {
    const data = GetServerLogsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetServerLogs", data);
    return promise.then((data) => GetServerLogsResponse.decode(new BinaryReader(data)));
  }

  ReadServerLog(request: ReadServerLogRequest): Promise<ReadServerLogResponse> {
    const data = ReadServerLogRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ReadServerLog", data);
    return promise.then((data) => ReadServerLogResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
